<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LibraryMe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/yellow_light</item>
        <item name="colorPrimaryDark" >?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar" >true</item>
        <item name="android:navigationBarColor">@color/yellow_light</item>
    </style>

    <style name="Theme.LibraryMe" parent="Base.Theme.LibraryMe" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/orange_light</item>
    </style>

    <style name="Widget.BottomNavigationView" parent="ThemeOverlay.Material3.NavigationView">
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <style name="HintTextStyle" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">15sp</item>
    </style>

</resources>